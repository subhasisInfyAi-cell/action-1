name: My First Workflow

on: push

jobs: 
    first_jobs:
        runs-on: ubuntu-latest
        steps: 
        - name: Welcome message
          run: echo 'My First GitHu Actions Job'
          
        - name: List files
          run: ls

        - name: Read file
          run:  cat README.md